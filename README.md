# 새마을금고 학습 · RC4 v0.24.1

2026-09-08 기준 최신 안정성 개선본. v0.24의 학습·기록 안정성 개선을 유지하면서 **INTRANET의 비실행 호스트 화면 색상과 실행 패널 색상을 분리**했다. GitHub 앱 반영 커밋: `bbda1eaa4ff1b3b683da655f8158a2231b8a9a27`.

## 실행
압축을 풀고 `index.html`을 연다. 별도 로그인은 없다. 사내판은 `03_INTRANET_PC.html` 한 파일로 실행하며 외부 통신을 사용하지 않는다. 기존 기록은 업데이트 전에 앱의 기록 메뉴에서 JSON으로 내보내기를 권장한다.

## INTRANET 최종 시각 기준
- **비실행 영역(좌측·상단·호스트 프레임)**: 이전 사내화면의 원래 색상 사진을 유지한다.
- **실제 학습 실행영역(.main)**: 현재의 회색/중성 업무형 UI를 유지한다.
- v0.24에 있던 `#app` 전체 grayscale 필터는 삭제했다. 따라서 호스트 화면까지 회색으로 바뀌지 않는다.
- 내장 호스트 배경 이미지는 이전 v0.6과 SHA256이 동일하다: `3bea84654e733a6b45a887e139cd8f528eff90e11b1d237bb8897dfaf020e097`.

## 보존한 학습 구조
1단계 10문항 필수 → 2단계 기본 40문항. Quick 52 / Focus 208 / 실제 제공 260 / Daily 5 / 개념 58 / 모의고사 20. Master 300에는 예비 40문항이 포함된다.

## 검증
Chromium DOM 자동 검사 **171/171 통과**. 별도 색상 경계 검사에서 실행 패널 내부 샘플 영역 비회색 픽셀 0, 호스트 상단·좌측·우측 영역의 색상 유지 확인. 앱 3종의 구문·260문항 CSV 정합성·외부 런타임 통신 0도 통과했다.

실제 Galaxy·사내 PC·실제 파일/웹 주소의 영구 저장과 법령·내부규정 300문항의 실질 최신성 감수는 별도 확인 항목이다.

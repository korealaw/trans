# 새마을금고 학습 · RC4 v0.24

2026-09-08 기준 안정성 개선본. 앱 본체 3종을 GitHub `main`에 반영하고 로컬 시험본과 Git blob SHA 일치를 확인했다. 앱 반영 커밋: `0c0abfe47e814b9c3edbde6043d264da22cf2db7`.

## 실행
압축을 풀고 `index.html`을 연다. 별도 로그인은 없다. 사내판은 `03_INTRANET_PC.html` 한 파일로 실행하며 외부 통신을 사용하지 않는다. 기존 기록은 업데이트 전에 앱의 기록 메뉴에서 JSON으로 내보내기를 권장한다. 기기·브라우저·접속 주소가 달라지면 기록은 자동 동기화되지 않는다.

## 보존한 학습 구조
1단계 10문항 필수 → 2단계 기본 40문항. Quick 52 / Focus 208 / 실제 제공 260 / Daily 5 / 개념 58 / 모의고사 20. Master 300에는 예비 40문항이 포함된다. 340문항으로 합산하지 않는다. 원자료 PDF는 공개 저장소에 올리지 않는다.

## 이번 수정
미응답 진행 차단, 집중학습 전용 문항의 Quick 유입 차단, 즉시 재출제 문항당 1회 제한, 완료 기록 보존, 재개 화면 저장 순서 수정, 마지막 정답 반영, 손상된 기록 복구와 원본 보관, 가져오기·초기화 전 백업, 저장 실패·다른 창 충돌 표시, 모의고사 중복 채점 방지, 사내판 회색조·스크롤 보완.

## 검증과 한계
Chromium DOM 자동 검사 171개 통과. 저장소는 명시적인 테스트 대역을 사용했으므로 실제 Galaxy·사내 PC·실제 파일/웹 주소의 영구 저장까지 인증한 결과가 아니다. `qa/verify_release.py`는 시험본 해시·구문·260문항 CSV 정합성을 검사한다. 법령·내부규정 300문항의 실질적인 최신성 감수는 별도 미완료 과제다. 상세 결과는 `RC4_V024_AUDIT.md`를 본다. C9/C23 문서는 과거 기록이며 최신 기준이 아니다.

## 유지보수
`maintenance/v024_*.js`는 빌드 시 원래 함수 정의를 대체한다. 실행 중 중복 override 방식이 아니다. `apply_v024.py`는 알려진 v0.23 해시에서만 작동하고 다른 입력은 중단한다. 이미 v0.24이면 `python qa/verify_release.py`로 확인한다. `.github/workflows/rebuild-v024.yml`은 시험본과 일치하는 경우에만 앱 3종을 한 커밋으로 반영한다.

공개 사이트 배포와 코드 반영은 별도다. GitHub Pages 설정은 저장소의 Settings → Pages → Deploy from a branch → main / (root) → Save에서 처리한다.

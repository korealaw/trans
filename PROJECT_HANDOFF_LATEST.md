# PROJECT_HANDOFF_LATEST — 새마을금고 학습

## 현재 기준 · 2026-09-08
제품: 새마을금고 학습. 저장소: `korealaw/trans`, 기준 브랜치: `main`. 최신 앱: **RC4 v0.24 안정성 개선본**. 앱 반영 커밋: `0c0abfe47e814b9c3edbde6043d264da22cf2db7`. GitHub Actions 실행 `34168025041`에서 재생성·정합성 검사·앱 커밋 성공을 확인했다. 3종 HTML의 Git blob SHA가 로컬 시험본과 동일하다. 공개 Pages 활성화 및 실제 기기 배포 확정과는 구분한다.

## 반드시 유지
제품명은 새마을금고 학습으로 통일. 로그인 추가 금지. 삭제한 마인드맵 복원 금지. 1단계 Quick 10문항 필수 → 2단계 Focus 기본 40문항. Quick/Focus 전용 문제군 비중복. PHONE 대화형 UI 유지. INTRANET 회색조·작은 업무형 글씨·사각형 버튼·단일 main 스크롤·외부 통신 없음. 공식 원자료 PDF 10종 공개 업로드 금지.

## 데이터와 기존 기록
활성 260 = Quick 52 + Focus 208. Daily 5개(40/38/42/42/46), 개념 58, 모의 20(Focus 전용). Master 300에 Reserve 40 포함. Reserve를 더해 340으로 계산하지 않는다. 단일문항 개념 13, Quick 전용 문항이 없는 개념 21.

문항·정답·보기별 해설과 정답 순서를 바꾸지 않았다. CSV answer는 1부터, HTML answer는 0부터. `FLOW_SCHEMA_VERSION=23`, `DAILY_SCHEMA_VERSION=3`, course id와 localStorage KEY는 호환성을 위해 유지했다. 새 버전 번호와 저장 스키마 번호가 다른 것은 의도적이다.

## 수정 완료
미응답/중복 진행 방어, 저장 후 화면변경 순서 오류 수정, 완료 Daily를 복습해도 완료 기록 유지, 즉시 반복 문항당 1회 제한, 마지막 응답으로 취약상태 갱신, 손상기록 정규화/원본 보관, 가져오기와 초기화 전 복구 가능한 백업, 미래 스키마 덮어쓰기 차단, 다른 창 기록 충돌·저장 실패 경고, 모의고사 누락답안·중복 채점 방어, 날짜 변경 시 흐름 정리, 남은 20문항 미만 목표 보존, 재개 시 미완료 반복학습 보존.

Quick 회복은 Quick 전용 문제군 안에서만 수행한다. 전용 문항이 없는 개념은 해당 Daily의 Focus 회복에 배치한다. 같은 문항 반복정답·단일문항 개념만으로 MASTERED로 만들지 않는다.

## 검증 수준
로컬 Chromium DOM 검사 171개 통과, 미처리 오류 0. 208문항 전 범위 완주·Daily 간 연결·백업 FileReader 가져오기 포함. 저장 기능은 명시적 localStorage 테스트 대역을 사용했다. 실제 Galaxy, 실제 사내 PC, 네이티브 file/HTTP 저장 지속성은 미확인이다. 3종 구문 검사·중복 함수 0·260문항 CSV 정합성·외부 런타임 통신 0 확인. 사내판 홈/학습지도 캡처의 비회색 픽셀 0 확인.

## 다음 작업
1. GitHub 최신 main과 `qa/static_results.json`을 먼저 확인한다. 현재 Pages 활성화 여부를 다시 읽고 공개 URL을 확인한다. 페이지 비활성화가 코드 무결성을 증명하지는 않는다.
2. `C24_DEVICE_SELFTEST.html`로 실제 Galaxy와 사내 PC에서 재시작 후 기록·모의고사·스크롤·오프라인 실행을 확인한다. 사용자가 준 결과가 없으면 실제 기기 PASS로 기록하지 않는다.
3. 콘텐츠는 공식 원문과 시험 기준일을 대조해 감수한다. 법령 실질 최신성 전수 검증은 이번 코드 QA와 별개이며 미완료다. 13개 단일문항 개념의 대체문항을 보강하되 확인 없이 활성화하지 않는다. Reserve 40은 기존 Master 안의 후보이므로 신규 수량으로 중복 합산하지 않는다.

## 유지보수/재현
`maintenance/v024_*.js` 4종과 `apply_v024.py`로 알려진 v0.23 원본을 결정론적으로 수정했다. 대용량 payload 분할 전송이나 실행 시 함수 덮어쓰기가 아니다. `qa/verify_release.py`에서 시험본 해시가 다르면 배포를 중단한다. 전체 인계 ZIP에는 `qa/test_browser.py`, `qa/cases.json`, 상세 결과 JSON과 화면 캡처가 포함된다.

C9, C23 및 RC4_V023_FINAL_QA는 과거 기록이다. 과거 ‘코드는 정상이고 Pages만 문제’라는 결론은 이번 재현 결과로 정정한다. 실제 코드 오류와 배포 문제를 분리해서 판단한다.

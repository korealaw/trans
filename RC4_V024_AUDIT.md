# 새마을금고 학습 RC4 v0.24 점검·수정 보고서

검토일: 2026-09-08. 대상: 사용자 첨부 trans-main.zip 및 korealaw/trans의 main.

## 1. 결론과 이전 판단 정정
이전의 ‘소스에는 문제가 없고 GitHub Pages만 문제’라는 단정은 지나쳤다. 정적 구문 검사는 통과했지만 실제 상태 전환·반복·기록 복구에는 오류가 있었다. 이번에는 원본에서 오류를 재현한 다음 수정본으로 다시 검사했다. v0.24 앱 본체 3종은 GitHub main에 반영되었고 로컬 시험본과 바이트 해시가 일치한다. 이것은 실제 모든 기기·모든 입력에서의 무오류 보증이나 법령 실질 감수 완료를 의미하지 않는다.

## 2. 재현한 문제와 조치
| 문제 | 원본에서 확인한 동작 | v0.24 조치 |
|---|---|---|
| 미응답 진행 | nextQuick 호출로 미응답 다음 문항 이동 | 응답·화면·완료 상태를 함수 안에서도 확인 |
| 미응답 완료 처리 | 답하지 않고 rateLearn 호출 시 완료 수 증가 | 유효 응답과 정해진 평가값을 확인 |
| 문제군 혼입 | Focus 전용 QRC2-109가 Quick 회복 큐로 유입 | Quick/Focus 회복 후보를 전용 집합으로 제한 |
| 재개 위치 저장 | 집중학습 시작 후 저장값은 dailyIntro에 잔류 | 화면·현재 Daily 설정 후 저장 |
| 끝나지 않는 복습 | 단일문항을 20회 반복한 뒤에도 대기열 증가 | 같은 세션 내 즉시 반복을 문항당 1회로 제한 |
| 완료 기록 소실 | 완료 Daily 재학습 시 done 초기화 | 완료 기록 유지, 복습 세션 수만 별도 계산 |
| 영구 취약 판정 | 오답 후 정답이어도 과거 오답 때문에 계속 취약 | 마지막 정오·평가와 개념 회복 상태로 판단 |
| 손상된 기록 | done이 숫자이거나 모의 인덱스가 범위 밖이면 예외 | 타입·범위·허용 ID 검증, 원본 보관, 안전한 화면 복구 |
| 저장 실패 은폐 | 저장 실패를 사용자에게 알리지 않음 | 화면 경고와 내보내기 제공 |
| 가져오기·초기화 위험 | 기존 기록에 대한 안전장치 부족 | 형식·과정·버전 확인, 확인창, 직전 복구본 저장 |
| 다중 창 충돌 | 다른 창의 새 기록을 덮어쓸 위험 | 변경 감지 후 저장 중지와 최신 기록 불러오기 |
| 날짜·목표·재시작 | 오래된 세션 또는 마지막 소량 목표 복원 불일치 | 날짜별 흐름 정리, 남은 목표 및 반복 복습 복원 |
| 모의고사 | 불완전 저장값과 중복 채점 방어 부족 | 유효 20답안 확인, 1회 채점, 이전 문항 이동 |
| 사내판 색상 | CSS 검사만으로 내장 이미지의 색상까지 보장 불가 | 전체 렌더링 회색조 처리, 캡처 픽셀 확인 |

## 3. 검사 결과
Chromium DOM 검사 **171개 통과 / 실패 0**. 51개 공통 시나리오를 PHONE·PERSONAL PC·INTRANET에 각각 적용하고 클릭 흐름, 파일 가져오기, 화면폭, 스크롤, 미처리 오류를 추가 검사했다. 208개 집중문항 전 범위 완주와 38문항 Daily 후 다음 Daily의 2문항 연결을 포함한다. 반복학습은 단일문항 2회 안에, 40개 모두 어려움 평가 시 80회 안에 해당 세션이 종료됨을 확인했다.

별도 정적 검사에서 JavaScript 구문 통과, 동일 이름 함수 중복 0, 3종 데이터 완전 동일, 활성 260문항의 질문·보기·정답·보기별 해설·단계별 설명·개념·Daily·암기 문구가 Master CSV와 일치함을 확인했다. 외부 런타임 네트워크 호출은 없었다. 사내판 홈과 학습지도 캡처에서는 RGB 채널 차이가 2를 초과한 비회색 픽셀이 0개였다.

### 검사 환경의 한계
실제 DOM과 JavaScript는 Chromium에서 실행했지만 테스트 환경 정책이 file/로컬 HTTP 탐색을 차단하여 HTML 내용을 주입하고 localStorage를 명시적으로 모의했다. 저장 실패·손상·충돌·가져오기 논리는 검증했으나 실제 브라우저 영구 저장, 실제 휴대전화, 실제 사내 보안정책을 통과했다는 의미는 아니다. `qa/browser_results.json` 및 `qa/test_browser.py`에 이 구분을 명시했다.

## 4. 데이터 수량과 콘텐츠 감수 범위
실제 제공 260 = Quick 52 + Focus 208. Master 300 = 활성 260 + 예비 40. 예비 CSV 40은 Master의 부분집합이다. **300+40=340으로 계산하면 중복 집계다.** Daily는 5개이며 집중문항 수는 40/38/42/42/46. 개념은 58개, 그중 단일문항 개념 13개, Quick 전용 문항이 없는 개념 21개다.

문제 본문·정답·해설·정답 순서는 이번에 변경하지 않았다. 300문항의 현행 법령·내부규정과의 실질 일치 및 시험 기준일 적합성은 아직 전수 감수하지 않았다. 출처 필드가 존재한다는 사실과 법적 내용이 현재도 맞는다는 판단은 구별해야 한다. 이 작업을 ‘최신 법령 반영 완료’로 표현하지 않는다.

## 5. 배포와 재현
GitHub Actions 실행 34168025041이 입력 해시 확인 → 앱 3종 생성 → 시험본 해시·데이터 정합성 확인 → 단일 커밋 반영까지 성공했다. 앱 반영 커밋은 0c0abfe47e814b9c3edbde6043d264da22cf2db7이다. 로컬과 GitHub의 3종 앱 해시가 동일하다. GitHub Pages 활성화 및 실제 공개 주소 확인은 코드 반영과 구분한다.

`python qa/verify_release.py`는 현재 배포본을 검사한다. `python qa/test_browser.py`는 Playwright와 /usr/bin/chromium이 있는 시험 환경에서 실행하며 명시적 저장 테스트 대역을 사용한다. apply_v024.py는 알려진 v0.23 원본에서만 작동한다. 무분별한 재적용과 다른 버전 덮어쓰기를 막기 위한 제한이다.

## 6. 남은 배포 승인 항목
실제 Galaxy에서 화면·재개·모의고사·백업, 실제 사내 PC에서 단일 HTML 오프라인 실행과 단일 스크롤, 실제 주소에서 브라우저 재시작 후 기록 보존, 법령·규정 및 시험 지정 자료 기준일 감수. 이 항목은 확인 결과가 오기 전까지 미확인으로 남긴다.

## 참고 문서
GitHub Pages: https://docs.github.com/en/pages/getting-started-with-github-pages/configuring-a-publishing-source-for-your-github-pages-site
Web Storage: https://developer.mozilla.org/en-US/docs/Web/API/Window/localStorage
GitHub 실행: https://github.com/korealaw/trans/actions/runs/34168025041

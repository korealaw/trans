# NEW_CHAT_START_HERE — 새마을금고 학습

새 채팅에서 사용자가 **`다음 작업 진행`**이라고만 입력하면 다음 절차를 따른다.

1. `PROJECT_HANDOFF_LATEST.md`와 `README.md`를 읽는다.
2. 현재 기준은 **RC4 v0.23 INTEGRITY UPDATE**다.
3. 사용자에게 프로젝트 설명을 다시 요구하지 않는다.
4. 확정 정책을 재논의하지 않는다.
5. GitHub 루트의 앱 본체 3개 존재 여부와 `.payload` 잔존 여부를 먼저 확인한다.
6. 앱 본체가 있으면 size/blob SHA → index 링크 → Pages 실URL 순으로 검증한다.
7. 앱 본체가 없으면 RC4 v0.23 기준으로 배포 준비를 계속한다.

상세 정책·데이터·QA·다음 단계는 반드시 `PROJECT_HANDOFF_LATEST.md`를 기준으로 한다.

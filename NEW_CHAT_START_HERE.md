# 새 채팅 시작

저장소는 `korealaw/trans`, 제품명은 **새마을금고 학습**이다. 최신 기준은 **RC4 v0.24.1**.

먼저 `PROJECT_HANDOFF_LATEST.md` → `qa/static_results_v0241.json` → 최신 `main` 순서로 확인한다.

핵심 시각정책: INTRANET의 좌측·상단·호스트 프레임은 이전 사진의 원색을 유지하고, 실제 학습 실행영역 `.main`만 회색 업무형 UI로 유지한다. 전체 `#app` grayscale 필터는 금지한다.

사용자가 `다음 작업 진행`이라고 하면 설명을 다시 요구하지 말고 Pages/실기기 검증부터 이어간다.
